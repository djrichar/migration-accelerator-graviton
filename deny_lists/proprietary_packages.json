{
  "$schema": "../schemas/deny_list_schema.json",
  "deny_list": [
    {
      "name": "mssql-server",
      "aliases": ["microsoft-sql-server"],
      "reason": "Microsoft SQL Server engine does not support ARM64/Graviton architecture (Linux x86_64 only). Note: MSSQL client drivers (mssql-jdbc, go-mssqldb, etc.) are compatible with ARM64.",
      "minimum_supported_version": null,
      "recommended_alternative": "postgresql"
    }
  ]
}
